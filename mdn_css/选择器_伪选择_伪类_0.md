# 伪类选择器
伪类选择器是css中用于选择**特定状态**的元素的选择器，而不是选择元素的类型或属性。它们以冒号（`:`）开头，后面跟着伪类名称，用户根据元素的**动态状态**（如鼠标悬停、获得焦点）或**结构位置**（如第一个子元素）来应用样式。

**核心作用**：让CSS能够根据元素的**状态**（而非静态结构）来应用样式，实现更丰富的交互体验。
**分类**：伪类选择器分为标准式和函数式，形象点就是`:伪类名称`和`:伪类名称()`