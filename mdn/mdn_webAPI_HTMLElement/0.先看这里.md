# **先看这里**
推荐先看这一篇，至少知道整体学习结构。
HTMLElement对标MDN文档讲解，属于表示所有HTML。**一些HTML元素直接实现**了HTMLElement接口，**其它的间接实现**了HTMLElement接口，因此关于自定义元素方面也需要实现了HTMLElement接口。然后如果实现了对应接口，调用了一些方法在此分类中找不到，去HTML

## **继承**
继承自Element接口，所以Element所有的实例属性，实例方法，事件都有